import { supabase } from "../config/supabase.js";

export async function updateUserCards(boosterPackCards, userID) {

    console.log(boosterPackCards);

    if (error) {
        console.error("fetch.js: Error fetching money:", error);
        return [];
    }
    return money;
}